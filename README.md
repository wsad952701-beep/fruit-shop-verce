# 🍇 果實搬運工 - Vercel Serverless Demo 版本

水果電商系統的 **Demo 模式** - 無需任何外部服務！

## 🚀 特色

- ✅ **完全免費** - 使用 Vercel 免費方案
- ✅ **無需註冊外部服務** - 不需要 Turso、不需要資料庫
- ✅ **不休眠** - Serverless 函數隨時可用
- ✅ **一鍵部署** - 推送到 GitHub 即可

## 📦 Demo 模式說明

本專案使用**記憶體內資料庫**：
- 資料在伺服器運行期間保持
- 伺服器重啟後自動載入預設資料
- 適合展示、Demo、作品集使用

## 🚀 部署步驟

### 1. 推送到 GitHub

```bash
cd fruit-shop-vercel
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/fruit-shop-vercel.git
git push -u origin main
```

### 2. 部署到 Vercel

1. 前往 [vercel.com](https://vercel.com/) → 用 **GitHub 登入**
2. 點「**New Project**」→ Import `fruit-shop-vercel`
3. 直接點「**Deploy**」（不需設定任何環境變數！）

完成！🎉

## 🔑 預設帳號

| 類型 | Email | 密碼 |
|------|-------|------|
| 管理員 | `admin@fruitporter.com` | `admin123` |
| 測試會員 | `demo@example.com` | `demo123` |

## 🌐 網站入口

部署後可訪問：

| 頁面 | URL |
|------|-----|
| 前端購物 | `https://your-app.vercel.app/` |
| 後台管理 | `https://your-app.vercel.app/admin/` |

## 📁 專案結構

```
fruit-shop-vercel/
├── public/              # 靜態檔案
│   ├── frontend/        # 前端購物網站
│   └── admin/           # 後台管理系統
├── api/                 # Serverless API
│   ├── auth/            # 認證 API
│   ├── products/        # 商品 API
│   ├── cart/            # 購物車 API
│   ├── orders/          # 訂單 API
│   ├── favorites/       # 收藏 API
│   ├── settings/        # 設定 API
│   └── admin/           # 管理員 API
├── lib/
│   ├── db.js            # 記憶體內資料庫（自動載入資料）
│   └── auth.js          # 認證輔助函數
├── vercel.json          # 路由配置
└── package.json
```

## ⚠️ 注意事項

- 這是 **Demo 模式**，資料不會永久保存
- 每次函數冷啟動時會重新載入預設資料
- 如需持久資料，請使用 Turso 或其他資料庫服務
